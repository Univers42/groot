# Vault session operation targets — Vault tooling moved to the standalone apps/grobase stack.
vault-login-fly-admin:
## Deprecated at root: Vault session admin now lives in apps/grobase.
	@echo "[skip] vault-login-fly-admin now lives in apps/grobase"

vault-session-status:
## Deprecated at root: Vault session status now lives in apps/grobase.
	@echo "[skip] vault-session-status now lives in apps/grobase"

vault-get-secrets:
## Deprecated at root: Vault secret fetch now lives in apps/grobase.
	@echo "[skip] vault-get-secrets now lives in apps/grobase"

vault-kv-export:
## Deprecated at root: Vault KV export now lives in apps/grobase.
	@echo "[skip] vault-kv-export now lives in apps/grobase"

vault-logout:
## Deprecated at root: Vault session logout now lives in apps/grobase.
	@echo "[skip] vault-logout now lives in apps/grobase"
