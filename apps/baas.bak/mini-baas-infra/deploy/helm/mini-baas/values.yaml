# GENERATED by scripts/gen-deploy.py from the Makefile manifest + docker-compose.yml.
# Do not edit by hand — run `make deploy-gen`.
edition: lean
global:
  containerSecurityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL
    privileged: false
  imagePullPolicy: IfNotPresent
  imageRegistry: ghcr.io/les-baas
  imageTag: latest
  nameOverride: mini-baas
  podSecurityContext:
    fsGroup: 10001
    runAsGroup: 10001
    runAsNonRoot: true
    runAsUser: 10001
    seccompProfile:
      type: RuntimeDefault
namespace: ''
namespaceDefault: mini-baas
networkPolicy:
  enabled: true
  ingressControllerNamespace: ingress-nginx
  ingressTargets:
  - kong
  - waf
services:
  adapter-registry-go:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-adapter-registry-go:latest
    ports:
    - 3021
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 2000m
        memory: 192Mi
      requests:
        cpu: 1000m
        memory: 0Mi
  ai-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-ai-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3100/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  analytics-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-analytics-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3070/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  cockroach:
    enabled: false
    image: cockroachdb/cockroach:v24.3.5
    ports:
    - 8080
    - 26257
    probe:
      exec:
      - cockroach
      - sql
      - --insecure
      - --host=127.0.0.1:26257
      - -e
      - SELECT 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 0Mi
  data-plane-router-rust:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-data-plane-router-rust:latest
    ports:
    - 4011
    probe:
      exec:
      - /app/data-plane-router
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    - rust-data-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
  db-bootstrap:
    enabled: true
    image: pgvector/pgvector:pg16
    ports: []
    profiles: []
    replicas: 1
  debezium:
    enabled: false
    image: quay.io/debezium/server:3.0
    ports: []
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8080/q/health >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  email-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-email-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3030/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  function-scheduler:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-function-scheduler:latest
    ports:
    - 3027
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - background
    - functions
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
  functions-runtime:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-functions-runtime:latest
    ports:
    - 3060
    probe:
      exec:
      - deno
      - eval
      - try{const r=await fetch('http://localhost:3060/health/live');Deno.exit(r.ok?0:1)}catch{Deno.exit(1)}
      failureThreshold: 5
      initialDelaySeconds: 10
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - functions
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  gdpr-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-gdpr-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3080/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  gotrue:
    enabled: true
    image: supabase/gotrue:v2.188.1
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:9999/health || exit 1
      failureThreshold: 8
      initialDelaySeconds: 15
      periodSeconds: 5
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  grafana:
    enabled: false
    image: grafana/grafana:10.4.2
    ports:
    - 3000
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:3000/api/health >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
  iceberg-rest:
    enabled: false
    image: apache/iceberg-rest-fixture:1.8.1
    ports:
    - 8181
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8181/v1/config >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  kong:
    enabled: true
    image: kong:3.8
    ports:
    - 8000
    - 8001
    probe:
      exec:
      - sh
      - -c
      - kong health
      failureThreshold: 10
      initialDelaySeconds: 20
      periodSeconds: 5
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 1024Mi
      requests:
        cpu: 500m
        memory: 0Mi
  log-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-log-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3110/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  loki:
    enabled: false
    image: grafana/loki:3.0.0
    ports:
    - 3100
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3100/ready >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 0Mi
  mariadb:
    enabled: false
    image: mariadb:11
    ports:
    - 3306
    probe:
      exec:
      - healthcheck.sh
      - --connect
      - --innodb_initialized
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  minio:
    enabled: false
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
    ports:
    - 9000
    - 9001
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:9000/minio/health/live >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - extras
    - storage
    replicas: 1
  minio-iceberg-init:
    enabled: false
    image: minio/mc:RELEASE.2025-04-08T15-39-49Z
    ports: []
    profiles:
    - analytics
    - extras
    - storage
    replicas: 1
  mongo:
    enabled: false
    image: mongo:7
    ports:
    - 27017
    probe:
      exec:
      - sh
      - -c
      - 'mongosh --quiet --eval "try { rs.status().myState === 1 ? quit(0) : quit(1) } catch(e) { db.runCommand({
        ping: 1 }).ok ? quit(0) : quit(1) }"'
      failureThreshold: 10
      initialDelaySeconds: 15
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - data-plane
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 0Mi
  mongo-api:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-mongo-api:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3010/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - data-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  mongo-init:
    enabled: false
    image: mongo:7
    ports: []
    profiles:
    - data-plane
    replicas: 1
  mongo-keyfile:
    enabled: false
    image: mongo:7
    ports: []
    profiles:
    - data-plane
    replicas: 1
  mssql:
    enabled: false
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
    - 1433
    probe:
      exec:
      - sh
      - -c
      - /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "$$MSSQL_SA_PASSWORD" -C -No -Q 'SELECT 1'
        -b -o /dev/null
      failureThreshold: 10
      initialDelaySeconds: 45
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - engines-extra
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 2048Mi
      requests:
        cpu: 500m
        memory: 0Mi
  mysql:
    enabled: false
    image: mysql:8.4
    ports:
    - 3306
    probe:
      exec:
      - mysqladmin
      - ping
      - -h
      - 127.0.0.1
      - -u
      - root
      - -p${MYSQL_ROOT_PASSWORD:-mysqlroot}
      failureThreshold: 5
      initialDelaySeconds: 20
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - analytics
    - data-plane
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  newsletter-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-newsletter-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3090/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  orchestrator:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-orchestrator:latest
    ports:
    - 3026
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - background
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 32Mi
      requests:
        cpu: 125m
        memory: 0Mi
  outbox-relay:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-outbox-relay:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3130/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  permission-engine:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-permission-engine:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3050/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    - control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  pg-backup:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-pg-backup:latest
    ports: []
    probe:
      exec:
      - /opt/pg-backup/entrypoint.sh
      - liveness
      failureThreshold: 3
      initialDelaySeconds: 10
      periodSeconds: 30
      timeoutSeconds: 5
    profiles:
    - backups
    - ops
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  pg-meta:
    enabled: false
    image: supabase/postgres-meta:v0.91.0
    ports: []
    probe:
      exec:
      - sh
      - -c
      - node -e "fetch('http://localhost:8080/health').then(r=>{if(r.status!==200)throw 1}).catch(()=>process.exit(1))"
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - control-plane
    - extras
    replicas: 1
  playground:
    enabled: false
    image: nginx:1.27-alpine
    ports:
    - 80
    profiles:
    - playground
    replicas: 1
  postgres:
    enabled: true
    image: ghcr.io/les-baas/mini-baas-postgres:latest
    ports:
    - 5432
    probe:
      exec:
      - sh
      - -c
      - pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-postgres}
      failureThreshold: 5
      initialDelaySeconds: 90
      periodSeconds: 5
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 1000m
        memory: 512Mi
      requests:
        cpu: 500m
        memory: 0Mi
  postgrest:
    enabled: true
    image: postgrest/postgrest:v12.2.3
    ports: []
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  prometheus:
    enabled: false
    image: prom/prometheus:v2.52.0
    ports:
    - 9090
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:9090/-/healthy >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 15
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - observability
    replicas: 1
  promtail:
    enabled: false
    image: grafana/promtail:3.0.0
    ports: []
    profiles:
    - observability
    replicas: 1
  query-router:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-query-router:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:4001/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - adapter-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  realtime:
    enabled: false
    image: dlesieur/realtime-agnostic:0.2.1
    ports: []
    probe:
      exec:
      - /app/realtime-server
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - data-plane
    - realtime
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  redis:
    enabled: true
    image: redis:7-alpine
    ports:
    - 6379
    probe:
      exec:
      - redis-cli
      - ping
      failureThreshold: 5
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 512Mi
      requests:
        cpu: 125m
        memory: 0Mi
  schema-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-schema-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3060/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  session-service:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-session-service:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3120/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - legacy-node
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  storage-router:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-storage-router:latest
    ports: []
    probe:
      exec:
      - sh
      - -c
      - wget -qO- http://localhost:3040/health/live || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - extras
    - storage
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 128Mi
      requests:
        cpu: 125m
        memory: 0Mi
  studio:
    enabled: false
    image: supabase/studio:2026.03.30-sha-12a43e5
    ports:
    - 3000
    probe:
      exec:
      - sh
      - -c
      - 'node -e "fetch(''http://127.0.0.1:3000/api/profile'').then(r => process.exit(r.status < 500 ?
        0 : 1)).catch(() => process.exit(1))"'
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
    profiles:
    - control-plane
    - extras
    - studio
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 384Mi
      requests:
        cpu: 250m
        memory: 0Mi
  supavisor:
    enabled: false
    image: supabase/supavisor:2.7.4
    ports: []
    probe:
      exec:
      - sh
      - -c
      - curl -sSfL --head -o /dev/null http://127.0.0.1:4000/api/health || exit 1
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - control-plane
    - extras
    replicas: 1
  tenant-control:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-tenant-control:latest
    ports:
    - 3022
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 160Mi
      requests:
        cpu: 250m
        memory: 0Mi
  trino:
    enabled: false
    image: trinodb/trino:467
    ports: []
    probe:
      exec:
      - sh
      - -c
      - curl -fsS http://localhost:8080/v1/info >/dev/null || exit 1
      failureThreshold: 5
      initialDelaySeconds: 60
      periodSeconds: 15
      timeoutSeconds: 5
    profiles:
    - analytics
    - extras
    replicas: 1
    resources:
      limits:
        cpu: 2000m
        memory: 2048Mi
      requests:
        cpu: 1000m
        memory: 0Mi
  vault:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-vault:latest
    ports:
    - 8200
    probe:
      exec:
      - sh
      - -c
      - vault status -address=http://127.0.0.1:8200 2>/dev/null | grep -q 'Sealed.*false' || exit 1
      failureThreshold: 10
      initialDelaySeconds: 15
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - control-plane
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  vault-init:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-vault-init:latest
    ports: []
    profiles:
    - control-plane
    replicas: 1
  waf:
    enabled: true
    image: ghcr.io/les-baas/mini-baas-waf:latest
    ports:
    - 80
    - 443
    probe:
      exec:
      - sh
      - -c
      - grep -qi ':0050 .* 0A' /proc/net/tcp || exit 1
      failureThreshold: 3
      initialDelaySeconds: 5
      periodSeconds: 10
      timeoutSeconds: 3
    profiles: []
    replicas: 1
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 0Mi
  webhook-dispatcher:
    enabled: false
    image: ghcr.io/les-baas/mini-baas-webhook-dispatcher:latest
    ports:
    - 3025
    probe:
      exec:
      - /app/app
      - --healthcheck
      failureThreshold: 5
      initialDelaySeconds: 30
      periodSeconds: 5
      timeoutSeconds: 3
    profiles:
    - background
    - control-plane
    - go-control-plane
    replicas: 1
    resources:
      limits:
        cpu: 250m
        memory: 96Mi
      requests:
        cpu: 125m
        memory: 0Mi
