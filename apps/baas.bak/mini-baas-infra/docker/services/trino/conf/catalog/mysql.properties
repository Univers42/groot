# File: docker/services/trino/conf/catalog/mysql.properties
# Trino MySQL catalog (M2 federation).
# The companion `mysql` service is brought up via the `extras` compose profile.
connector.name=mysql
connection-url=jdbc:mysql://mysql:3306
connection-user=${ENV:MYSQL_USER}
connection-password=${ENV:MYSQL_PASSWORD}
